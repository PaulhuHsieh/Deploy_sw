#!/usr/bin/python
import urllib2
import subprocess
import sys
import signal
import os
from time import sleep
#td_str = "port " + sys.argv[1] 
#p2 = subprocess.Popen(["tcpdump", "-i", "eth1", td_str ], stdout=subprocess.PIPE )
#for row in iter(p2.stdout.readline, b''):
#    print row.rstrip()   # process here


#sleep(1)
#subprocess.Popen(["kill", p2.pid])
#output,err = p2.communicate()
#print output
#os.kill( p2.pid, signal.SIGKILL )
#print p2.pid

#f = open("tcpdump.txt", "w")
#f.write(output)
#f.close();

